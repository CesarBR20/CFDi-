/*
import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose';
import { Document } from 'mongoose';

@Schema()
export class AdminPreferences extends Document {
  @Prop({ required: true })
  settingName: string;

  @Prop({ required: true })
  settingValue: string;
}

export const AdminPreferencesSchema = SchemaFactory.createForClass(AdminPreferences);*/
